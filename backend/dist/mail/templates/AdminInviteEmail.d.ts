import * as React from "react";
interface AdminInviteEmailProps {
    inviteUrl: string;
    inviterName: string;
}
export declare const AdminInviteEmail: ({ inviteUrl, inviterName }: AdminInviteEmailProps) => React.JSX.Element;
export default AdminInviteEmail;
